import type { NextConfig } from "next";

// Keep the initial Next.js config small so we can add only what the project
// actually needs as the frontend architecture settles.
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
